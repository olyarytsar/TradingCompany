<Window x:Class="TradingCompany.WPF.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Trading Company - Login" Height="350" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">

    <Grid Margin="25">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="System Login" FontSize="24" FontWeight="Bold" 
                       HorizontalAlignment="Center" Margin="0,0,0,30"/>

            <TextBlock Text="Username:" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" 
                     Height="30" Margin="0,0,0,15" Padding="2"/>

            <TextBlock Text="Password:" Margin="0,0,0,5"/>
            <PasswordBox x:Name="UserPasswordBox" Height="30" Margin="0,0,0,25" Padding="2"/>

            <Button Content="Sign In" Height="40" FontSize="14" FontWeight="SemiBold"
                    Command="{Binding LoginCommand}" 
                    CommandParameter="{Binding ElementName=UserPasswordBox}"/>
        </StackPanel>
    </Grid>
</Window>