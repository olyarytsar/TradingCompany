<UserControl x:Class="TradingCompany.WPF.Windows.WarehouseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Search product:" VerticalAlignment="Center" 
                       Foreground="{StaticResource DarkTextBrush}" FontWeight="SemiBold" Margin="0,0,15,0"/>
            <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Grid.Column="2" Content="Refresh" Command="{Binding RefreshCommand}" Width="120" Margin="15,0,0,0"/>
        </Grid>
<StackPanel Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Right" Margin="0,0,140,0">
    
</StackPanel>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Products}" 
                  SelectedItem="{Binding SelectedProduct}"
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                  BorderBrush="{StaticResource AccentBlueBrush}" BorderThickness="1" 
                  Background="White" RowHeight="30">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№" Binding="{Binding ProductId}" Width="60"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>

                <DataGridTextColumn Header="Supplier" Binding="{Binding Supplier.Brand}" Width="150"/>

                <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=C}" Width="100"/>
                <DataGridTextColumn Header="Stock" Binding="{Binding QuantityInStock}" Width="100"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category.CategoryName}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,15,0,0">
            <TextBlock Text="Total items: " Foreground="Gray"/>
            <TextBlock Text="{Binding Products.Count}" FontWeight="Bold" Foreground="{StaticResource DarkTextBrush}"/>
        </StackPanel>
    </Grid>
</UserControl>