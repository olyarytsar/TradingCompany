<UserControl x:Class="TradingCompany.WPF.Windows.ActiveOrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Active orders" FontSize="22" FontWeight="Bold" 
                   Foreground="{StaticResource DarkTextBrush}" Margin="0,0,0,20"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding ActiveOrders}" SelectedItem="{Binding SelectedOrder}"
                  AutoGenerateColumns="False" CanUserAddRows="False"
                  BorderBrush="{StaticResource AccentBlueBrush}" BorderThickness="1" Background="White" RowHeight="35">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№" Binding="{Binding OrderId}" Width="80" IsReadOnly="True"/>
                <DataGridTextColumn Header="Date" Binding="{Binding OrderDate, StringFormat=d}" Width="120" IsReadOnly="True"/>
                <DataGridTextColumn Header="Created By" Binding="{Binding Employee.FirstName}" Width="150" IsReadOnly="True"/>
                <DataGridTextColumn Header="Total" Binding="{Binding TotalAmount, StringFormat=N2}" Width="120"/>
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancel Order" Command="{Binding CancelOrderCommand}" 
                    Background="#FFEBEE" Foreground="{StaticResource ErrorBrush}" Width="140" Height="40" Margin="0,0,15,0"/>
            <Button Content="Save Changes" Command="{Binding SaveChangesCommand}" Width="140" Height="40"/>
        </StackPanel>
    </Grid>
</UserControl>